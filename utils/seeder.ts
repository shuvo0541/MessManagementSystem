
/**
 * Seeder functionality removed by user request.
 * File kept as placeholder to avoid import errors.
 */
export const generateStressTestData = (adminUserId: string, adminEmail: string): any => {
  return null;
};
